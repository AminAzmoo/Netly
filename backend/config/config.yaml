server:
  host: "0.0.0.0"
  port: 8081
  read_timeout: 60s
  write_timeout: 60s
  idle_timeout: 120s

database:
  host: "localhost"
  port: 5432
  user: "postgres"
  password: "1171"
  name: "netly_db"
  sslmode: "disable"
  max_idle_conns: 10
  max_open_conns: 100
  conn_max_lifetime: 1h

logger:
  level: "info"
  encoding: "json"
  output_paths:
    - "stdout"
  error_output_paths:
    - "stderr"

security:
  encryption_key: "change-me-in-production-32chars!"
  geoip_token: "5b089ad5d2a74086acffda1d70a30a57"
  public_url: "https://netly.example.com"

general:
  system_name: "Netly-Core-01"
  admin_email: "admin@netly.io"
  environment: "Production"

ipam:
  ipv4_cidr: "10.100.0.0/16"
  ipv6_cidr: "fd00::/8"

portam:
  min_port: 10000
  max_port: 60000

features:
  enable_locks: true
  request_id_header: "X-Request-Id"
  enable_task_correlation: true
  enable_request_logging: true

auth:
  admin_api_key: "change-me-admin"
  agent_token: "change-me-agent"
  allowed_origins:
    - "http://localhost:3000"
    - "http://localhost:5173"
    - "http://localhost:5174"
    - "https://panel.netly.local"
